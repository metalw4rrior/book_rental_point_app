# book_rental_point_app
A small application for working with the database of a book rental point.

Install **docker-compose** and write this: 

docker-compose up -d


Go to **http://localhost:5001**.
